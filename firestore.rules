rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Temporary test mode. Tighten before production use.
    match /{document=**} {
      allow read, write: if request.time < timestamp.date(2026, 5, 1);
    }
  }
}
