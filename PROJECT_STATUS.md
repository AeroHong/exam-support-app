# 프로젝트 현황표

작성일: 2026-04-01
프로젝트: 고사업무 지원APP

## 요약

현재 프로젝트는 시험 계획 MVP가 실행 가능한 상태이며, Google 로그인과 Firestore 연동 초안까지 들어가 있다. 다만 Firestore 운영 규칙과 실제 데이터 검증은 아직 완료되지 않았고, 자동화 테스트와 배포 준비도 남아 있다.

## 프로젝트 표

| 구분 | 항목 | 상태 | 현재 내용 | 다음 액션 |
| --- | --- | --- | --- | --- |
| 앱 기본 구조 | Vite + React + Tailwind 기반 앱 구성 | 완료 | 메인 앱 구조와 화면 구성이 잡혀 있음 | 유지 |
| 핵심 UI | 시험 카드, 달력 배치, 대시보드, 모달 구성 | 완료 | 카드 목록, 배치 영역, 시험실 배정, 학생 목록 모달 구현됨 | 실제 데이터 기준 점검 |
| 일정 계산 로직 | 충돌/대기/무시험/3연속/시험실 경고 계산 | 완료 | 엔진 훅 기반으로 계산 구조 존재 | 실제 업무 데이터로 정확도 검증 |
| 로그인 | Google 로그인 연동 | 진행중 | 로그인 자체는 연결됨 | Firebase 인증 설정과 실사용 계정 검증 |
| 테넌트 판별 | 학교 도메인 기반 tenant 매핑 | 완료 | `.env`의 도메인 맵으로 tenant 판별 | 학교 추가 시 매핑 확장 |
| 사용자 프로필 | Firestore `users` 문서 생성/갱신 | 진행중 | 코드 존재, 권한 문제 시 로컬 모드로 우회 | 운영용 Firestore 규칙 반영 |
| 공통 데이터 로딩 | 학생/수강/시험실 데이터 로딩 | 진행중 | Firestore 우선, 없으면 seed 데이터 사용 | 실데이터 컬렉션 구조 확정 및 검증 |
| 작업본 로딩 | 최신 시험 계획(plan) 불러오기 | 진행중 | 사용자별 최신 작업본 조회 구조 있음 | 실제 데이터 저장 후 재조회 검증 |
| 작업본 저장 | 시험 계획(plan) 저장 | 진행중 | Firestore 저장 코드 있음 | 저장 성공/실패 흐름 검증, 버전 정책 결정 |
| 권한 처리 | Firestore 권한 에러 대응 | 진행중 | 권한 부족 시 로그인 유지 + 로컬 모드 경고 표시 | 정식 보안 규칙 적용 후 제거 여부 판단 |
| Firestore 규칙 | 테스트 모드 규칙 파일 작성 | 진행중 | `firestore.rules`, `firebase.json` 추가됨 | Firebase에 실제 배포 |
| 운영 보안 | 운영용 Firestore Rules 설계 | 남음 | 아직 미구현 | tenant/role 기준 규칙 설계 |
| 역할 체계 | 관리자/교사/읽기 전용 권한 모델 | 남음 | 기본 role 값만 존재 | 역할 정책 정의 및 반영 |
| 예외 UX | 저장 실패, 권한 문제, 빈 데이터 처리 | 진행중 | 일부 에러 메시지 표시는 있음 | 사용자 친화적 안내 문구 보강 |
| 테스트 | 자동화 테스트 환경 | 남음 | 현재 수동 테스트만 가능 | Vitest 등 도입 |
| 빌드 검증 | 프로덕션 빌드 가능 여부 | 완료 | `npm run build` 성공 확인 | 지속 검증 |
| 개발 환경 | 로컬 개발 서버 실행 | 완료 | 로컬 접속 응답 확인 | 유지 |
| 콘솔 인코딩 | PowerShell UTF-8 출력 설정 | 완료 | 프로필에 UTF-8 설정 적용 | 새 세션에서 사용 |
| 성능 최적화 | 번들 크기 정리 | 남음 | 빌드 시 chunk size 경고 존재 | 코드 스플리팅 검토 |
| 배포 준비 | Firebase/호스팅/운영 환경 정리 | 남음 | 미진행 | 테스트/운영 환경 분리 후 배포 |

## 완료

- 앱 기본 골격 구성
- 주요 화면과 모달 구현
- 시험 일정 계산 로직 연결
- Google 로그인 연동
- 테넌트 판별 로직 연결
- Firestore 연동 초안 작성
- 빌드 및 개발 서버 실행 확인
- PowerShell UTF-8 설정 반영

## 진행중

- Firestore 기반 사용자/공통 데이터/작업본 연동
- Firestore 권한 문제 해결
- 테스트 모드 규칙 반영 준비
- 실제 데이터 기준 동작 검증

## 남음

- Firestore 규칙 실제 배포
- 운영용 보안 규칙 설계
- 역할 정책 확정
- 실제 학교 데이터 검증
- 저장/불러오기 정책 정리
- 자동화 테스트 도입
- 배포 준비
- 번들 크기 최적화

## 내일 바로 할 일 추천

1. Firebase 콘솔에 테스트 모드 Firestore Rules 반영
2. 학교 계정으로 다시 로그인 테스트
3. Firestore에 `users`, `tenants`, `plans` 데이터가 실제로 생성되는지 확인
4. 저장 후 재접속 시 작업본이 다시 불러와지는지 확인
5. 그 다음 운영용 Firestore Rules 설계 시작
