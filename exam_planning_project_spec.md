# 고사 계획 자동화 시스템 개발 계획서 (Claude Code / Codex 투입용)

## 1. 프로젝트 개요

### 1.1 목적
고등학교의 2, 3학년 선택과목 신청 현황에 따라 중복되지 않는 시험 시간표를 제작하고, 과목 배치에 따라 대기인원, 시험 미실시 인원 등 데이터 기반 의사결정을 지원하는 웹 기반 시스템을 구축한다.

이 시스템은 단순 시간표 작성 도구가 아니라, 시험 배치 결과를 실시간으로 분석하고 검증하여 학교 현장에서 실제 의사결정에 활용할 수 있는 운영 지원 플랫폼을 목표로 한다.

### 1.2 핵심 컨셉
- 자동 배치 시스템이 아니라 **반자동 배치 + 실시간 검증 시스템**
- 사용자가 시험 카드를 직접 배치하면, 시스템이 즉시 학생 충돌, 대기 인원, 무시험 인원, 연속 시험 인원, 고사실 수용 초과 여부 등을 계산한다.
- 학교별 도메인 단위로 데이터를 완전히 분리하여 멀티테넌트 구조로 운영한다.
- 사용자별 작업본 저장 및 불러오기 기능을 제공하며, 동일 학교 내 다른 사용자의 작업본을 복제하여 본인 작업본으로 사용할 수 있다.

---

## 2. 프로젝트 범위

### 2.1 주요 기능 범위
1. 고사 기초 정보 입력
2. 학생 명렬 및 선택과목 명렬 관리
3. 시험 카드 기반 일정 배치
4. 배치 결과 실시간 분석
5. 고사실 수동 배정 및 수용 인원 검증
6. 좌석 배치 자동 생성
7. 출력물 생성 및 PDF 다운로드
8. 사용자별 작업 저장/불러오기
9. 학교 도메인 기반 멀티테넌트 운영

### 2.2 제외 범위
- 자동 최적화 배치
- AI 추천 배치
- OR-Tools 기반 수학적 최적화
- 모바일 앱 별도 개발

단, 위 기능은 향후 확장 목표로 설계만 고려한다.

---

## 3. 기술 스택

### 3.1 Frontend
- React
- Tailwind CSS
- HTML 기반 UI 구성
- Firebase SDK

### 3.2 Backend
- Firebase Authentication
- Firestore
- Firebase Hosting
- Cloud Functions (필요 시)

### 3.3 출력/문서 생성
- HTML 템플릿 기반 인쇄/PDF 출력
- 필요 시 jsPDF 또는 html2pdf 검토

### 3.4 개발 방향
프론트는 단순 HTML만으로 유지하기 어렵기 때문에, 실제 구현은 아래 구조를 권장한다.

- UI 마크업: HTML 기반
- 스타일링: Tailwind CSS
- 상태관리 및 상호작용: React
- 백엔드 및 데이터 저장: Firebase

---

## 4. 핵심 업무 흐름

### 4.1 기본 운영 흐름
1. 로그인
2. 사용자 이메일 도메인으로 학교(tenant) 판별
3. 공용 마스터 데이터 로드
4. 사용자 작업본 생성 또는 기존 작업본 불러오기
5. 시험 카드 배치
6. 실시간 제약조건 검증 및 현황판 계산
7. 고사실 배정
8. 좌석 배치 생성
9. 출력물 생성 및 PDF 다운로드
10. 작업 저장

---

## 5. 시험 구조 및 운영 규칙

### 5.1 학년 운영 원칙
- 1, 2, 3학년은 완전히 독립적으로 운영한다.
- 충돌 계산도 학년별로 별도 처리한다.
- 단, UI에서는 하나의 통합 캘린더에서 1학년, 2학년, 3학년 열을 동시에 보여준다.

### 5.2 교시 정의 방식
- 교시는 시간 기반이 아니라 **당일 과목 편성 순서 기반**이다.
- 하루에 배치된 시험들을 시작 시각 기준으로 정렬한 후, 순서대로 1교시, 2교시, 3교시를 부여한다.
- 즉, 교시와 실제 시각은 요일마다 달라질 수 있다.

예시:
- 월요일: 08:30 시작 시험 = 1교시, 10:10 시작 시험 = 2교시
- 화요일: 09:00 시작 시험 = 1교시, 11:00 시작 시험 = 2교시

### 5.3 시간 편성 단위
- 시험 시작 시각은 5분 단위로 조정 가능해야 한다.
- 예: :00, :05, :10, :15 ...

### 5.4 점심시간
- 점심시간 블록은 두지 않는다.
- 시스템은 별도 점심시간 제약을 고려하지 않는다.

### 5.5 동일 과목 운영 규칙
- 동일 과목의 여러 타임 운영은 불가
- 동일 과목은 단일 시간에만 배치 가능
- 단일 시간 내 여러 고사실 사용은 가능

---

## 6. 제약조건 명세

### 6.1 학생 충돌 규칙
- 동일 학생은 같은 시간에 2개의 시험을 볼 수 없다.
- 같은 학년에서 시험 시간이 겹치더라도 학생이 겹치지 않으면 배치 가능하다.
- 즉, 시간 겹침 자체는 문제가 아니며 **학생 충돌이 절대 우선 조건**이다.

### 6.2 고사실 중복 규칙
- 동일 과목 내에서 한 학생이 서로 다른 고사실에 중복 배치될 수 없다.
- 동일 과목에 대해 고사실이 여러 개일 수 있으나, 학생은 반드시 하나의 고사실에만 속해야 한다.

### 6.3 학년별 겹침 규칙
- 학년 간 시간 겹침은 자유롭게 허용한다.
- 같은 학년 내에서도 학생 제약이 없으면 겹침 허용
- 학생 충돌이 발생하면 오류

### 6.4 동일 과목 분할 운영 제한
- 동일 과목을 서로 다른 시간에 나누어 운영하는 것은 금지
- 동일 시간 + 다수 고사실 구조만 허용

---

## 7. 학생 상태 정의

### 7.1 대기 인원
대기 인원은 **해당 교시에는 시험이 없지만 이후 교시에 시험이 있는 학생**으로 정의한다.

예시:
- 1교시 시험 없음, 2교시 시험 있음 → 대기
- 1교시 시험 있음, 2교시 시험 없음, 3교시 시험 있음 → 대기
- 1교시 시험 없음, 2교시 시험 없음, 3교시 시험 있음 → 대기

### 7.2 시험 미실시 인원
- 해당 날짜에 단 하나의 시험도 없는 학생

### 7.3 3과목 연속 시험 학생
- 당일 1교시, 2교시, 3교시에 모두 시험이 있는 학생

### 7.4 미배치 학생 / 미배치 과목
- 아직 어떤 시험 카드에도 연결되지 않은 학생 또는 과목
- 현황판에서 별도 경고 대상으로 표시 가능

---

## 8. 고사실 운영 규칙

### 8.1 배정 원칙
- 고사실 배정은 수동 배정이 원칙이다.
- 자동 고사실 분배 기능은 구현하지 않는다.

### 8.2 고사실 마스터 정보
각 고사실은 사전에 다음 정보를 가진다.

- 고사실명
- 기본 수용 인원
- 좌석 행 수
- 좌석 열 수
- 교탁 위치
- 출입구 위치

기본 좌석 구조:
- 6행 × 5열 (30석 기준)

### 8.3 수용 인원 검증
- 고사실 선택 후 총 수용 가능 인원이 응시인원보다 작으면 오류 표시
- 단, 임시로 1~2석 추가 허용 옵션 구현

검증 개념:
- 응시인원 > 총 수용인원 + 2 → 오류
- 응시인원 <= 총 수용인원 + 2 → 경고 또는 허용

### 8.4 고사실 배치 순서
- 학생은 고사실별로 순서대로 배정
- 남녀 구분 없음
- 반 구분 없음

---

## 9. 좌석 배치 규칙

### 9.1 기본 원칙
- 고사실별 학생 좌석 배치도를 자동 생성한다.
- 책상은 행-열 조합 구조를 사용한다.

### 9.2 기본 옵션
- 기본 구조: 6행 × 5열
- 시작 위치 설정 가능
- 교탁 표시
- 출입구 표시

### 9.3 배치 순서
- 학생 명단 순서대로 좌석에 배정
- 고사실 단위로 순차 배치

### 9.4 출력 목표
- 고사실별 학생배치도 PDF 생성
- 좌석 번호와 학생명 표시

---

## 10. 캘린더 UI 명세

### 10.1 전체 구조
하나의 캘린더 UI 안에서 다음 3개 열을 제공한다.

- 1학년
- 2학년
- 3학년

시간축은 공통으로 사용한다.

### 10.2 카드 표시 항목
각 시험 카드는 다음 정보를 반드시 표시한다.

- 과목명
- 과목코드
- 시험 시간
- 고사실
- 응시인원
- 서논술형 여부

### 10.3 카드 배치 규칙
- 드래그 앤 드롭으로 이동 가능
- 시작 시각 변경 시 5분 단위 스냅 적용
- 시험 시간에 따라 카드 길이 자동 조절
- 학년 간 겹침 허용
- 같은 학년 내 겹침도 학생 제약이 없으면 허용

### 10.4 카드 이동 시 시스템 동작
카드 이동 또는 수정 시 즉시 다음 계산 수행
- 학생 충돌 검사
- 대기 인원 계산
- 무시험 인원 계산
- 3연속 시험 학생 계산
- 고사실 수용 초과 여부 검사

---

## 11. 현황판(Dashboard) 명세

### 11.1 필수 지표
1. 당일 대기 학생 인원
2. 당일 무시험 학생 인원
3. 3과목 연속 시험 학생 인원

### 11.2 상호작용
- 각 숫자를 클릭하면 해당 학생 명단으로 이동 또는 모달 표시

### 11.3 추가 경고 지표
- 고사실별 책상 수 초과 여부
- 학생 충돌 발생 여부
- 미배치 과목 수
- 미배치 학생 관련 정보
- 기타 운영상 경고 정보

---

## 12. 입력 데이터 구조

### 12.1 고사 기초 정보
- 시험 진행 여부
- 시험 시간
- 서논술형 여부
- 과목코드
- 과목명
- 학년

### 12.2 학생 명렬
- 학번
- 이름
- 학년
- 반

### 12.3 선택과목 명렬
- 학생 ID
- 과목 ID

### 12.4 고사실 마스터
- 고사실명
- 수용 인원
- 좌석 구조
- 교탁 위치
- 출입구 위치

---

## 13. Firestore 데이터 모델 제안

### 13.1 멀티테넌트 구조
```text
tenants/{tenantId}
```

예시:
- doksan
- seonyoo

### 13.2 users
```text
users/{userId}
```
필드 예시:
- email
- tenantId
- role

### 13.3 students
```text
tenants/{tenantId}/students/{studentId}
```
필드 예시:
- studentNo
- name
- grade
- classNo

### 13.4 subjects
```text
tenants/{tenantId}/subjects/{subjectId}
```
필드 예시:
- name
- code
- grade
- isEssay

### 13.5 enrollments
```text
tenants/{tenantId}/enrollments/{enrollmentId}
```
필드 예시:
- studentId
- subjectId

### 13.6 rooms
```text
tenants/{tenantId}/rooms/{roomId}
```
필드 예시:
- name
- capacity
- rows
- cols
- teacherDeskPosition
- doorPosition

### 13.7 examSessions
```text
tenants/{tenantId}/examSessions/{examSessionId}
```
필드 예시:
- subjectId
- grade
- startTime
- duration
- roomIds[]
- studentCount

### 13.8 plans
```text
tenants/{tenantId}/plans/{planId}
```
필드 예시:
- ownerId
- name
- examSessionIds[]
- createdAt
- updatedAt
- sourcePlanId (복제 원본)
- status (draft / confirmed 등 추후 확장 가능)

---

## 14. 권한 및 멀티테넌트 정책

### 14.1 학교 도메인 기반 분리
- 로그인 이메일의 도메인을 기준으로 tenant를 판별한다.
- 사용자는 본인 도메인에 해당하는 학교 데이터만 조회/수정할 수 있다.

예시:
- @doksan.hs.kr → doksan 데이터만 접근 가능
- @seonyoo.hs.kr → seonyoo 데이터만 접근 가능

### 14.2 사용자별 작업본 분리
- 같은 학교 안에서도 저장된 작업본은 사용자별로 분리 관리한다.
- 동일한 시험 계획이라도 사용자마다 별도 저장된다.

### 14.3 다른 사용자 작업본 활용
- 다른 사용자의 결과물을 불러와서 본인 작업본에 덮어쓰는 기능 허용
- 단, 원본을 직접 수정하는 것이 아니라 복제 후 본인 작업본으로 저장하는 방식 권장

### 14.4 역할(Role) 구분
최소 3단계 권한을 고려한다.
- viewer: 조회만 가능
- editor: 작업본 생성/수정 가능
- admin: 마스터 데이터 및 공용 설정 관리 가능

---

## 15. 계산 엔진 설계

### 15.1 시스템 역할
이 시스템의 핵심은 자동 배치가 아니라 아래 역할이다.

- 제약조건 검증
- 학생 상태 계산
- 운영 위험 시각화
- 결과 출력 지원

### 15.2 주요 계산 항목
1. 학생 충돌 검사
2. 동일 과목 중복 배정 검사
3. 대기 학생 계산
4. 무시험 학생 계산
5. 3연속 시험 학생 계산
6. 고사실 수용 초과 여부 검사

### 15.3 교시 계산 방식
당일 배치된 시험을 시작 시각 기준 정렬 후 순서대로 1교시, 2교시, 3교시를 부여한다.

### 15.4 대기 계산 원칙
현재 교시에 시험이 없고 이후 교시에 시험이 존재하면 대기 상태로 본다.

### 15.5 시간 충돌 판정 원칙
시험 시간 겹침 자체는 허용되며, 같은 학생이 중복 포함될 때만 충돌로 처리한다.

---

## 16. 프론트엔드 구조 제안

### 16.1 디렉토리 구조 예시
```text
/frontend
  /components
    CalendarGrid
    ExamCard
    DashboardPanel
    RoomAssignModal
    StudentListModal
    SeatLayoutPreview
  /pages
    PlannerPage
    PrintPage
  /hooks
    useScheduleEngine
    useTenantData
  /utils
    timeUtils
    conflictChecker
    studentStatusCalculator
    roomValidator
```

### 16.2 주요 컴포넌트
- CalendarGrid: 시간축 + 학년별 컬럼 렌더링
- ExamCard: 시험 카드 UI
- DashboardPanel: 현황판
- RoomAssignModal: 고사실 배정
- StudentListModal: 대기/무시험/연속 시험 명단 표시
- SeatLayoutPreview: 좌석 배치 미리보기

### 16.3 핵심 Hook
`useScheduleEngine()`
반환 데이터 예시:
- conflicts
- waitingStudents
- noExamStudents
- tripleStudents
- roomWarnings
- unassignedSubjects

---

## 17. 출력물 기능 명세

### 17.1 전체 시험 일정표
- 날짜별/학년별 전체 운영표
- 출력 및 PDF 다운로드

### 17.2 고사실별 응시현황
- 고사실명
- 과목명
- 응시 인원
- 학생 명단

### 17.3 고사실별 좌석 배치도
- 좌석 구조 표시
- 교탁/출입구 표시
- 학생 이름 또는 번호 표시

### 17.4 학생 개인별 시험 시간표
필수 정보:
- 학생명
- 학번
- 과목명
- 일시
- 시험시간
- 장소

### 17.5 과목별 응시 명렬
- 과목별 응시자 목록 출력

---

## 18. 데이터 입력/불러오기 기능

### 18.1 초기 데이터 업로드
필수 업로드 대상:
- 학생 명렬
- 선택과목 명렬
- 과목 목록
- 고사실 마스터

### 18.2 시험 계획 저장/불러오기
- 사용자 작업본 저장
- 기존 작업본 불러오기
- 다른 사용자 작업본 복제 후 본인 작업본으로 저장

### 18.3 과거 시험 정보 재사용
- 이전 시험 계획을 불러와 새 계획의 초기값으로 사용할 수 있어야 함

---

## 19. MVP 범위 제안

### Phase 1
- 로그인 / 도메인 기반 tenant 분리
- 학생/과목/선택과목/고사실 데이터 입력
- 캘린더 기반 시험 카드 수동 배치
- 실시간 제약조건 검증
- 현황판 표시
- 작업 저장/불러오기

### Phase 2
- 고사실 배정
- 고사실 수용 검증
- 학생 명단 출력
- 전체 운영표 출력
- 학생 개인 시간표 출력
- PDF 다운로드

### Phase 3
- 좌석 자동 배치
- 좌석 배치도 출력
- 작업본 비교
- 확정안/초안 구분
- 감사 로그

---

## 20. 향후 확장 아이디어

- 자동 추천 배치
- OR-Tools 기반 최적화
- 전년도 계획 복제 자동화
- 모바일 조회 화면
- 감독표/운영표 자동 생성
- 학교별 커스텀 규칙 엔진
- Google Sheets 연동 업로드/내보내기
- Apps Script 기반 보조도구 연동

---

## 21. Claude Code / Codex 작업 지시 요약

이 프로젝트에서 Claude Code / Codex는 아래 순서로 개발을 시작해야 한다.

1. React + Tailwind + Firebase 기본 프로젝트 스캐폴딩 생성
2. Firebase Auth 및 tenant 판별 구조 구현
3. Firestore 스키마 반영
4. 시험 카드 캘린더 UI 구현
5. Drag & Drop 기반 카드 배치 기능 구현
6. 실시간 계산 엔진 구현
7. Dashboard 구현
8. 고사실 배정 모달 구현
9. 출력 페이지 및 PDF 기능 구현
10. 저장/불러오기 및 작업본 복제 기능 구현

---

## 22. 핵심 개발 철학

이 시스템은 자동 배치 엔진이 아니다.  
사람이 배치한 시험 계획을 실시간으로 검증하고, 운영 리스크를 수치와 명단으로 보여주는 **시험 운영 분석 플랫폼**이다.

따라서 개발 우선순위는 다음과 같다.

1. 배치의 자유도 확보
2. 제약조건 검증 정확성 확보
3. 현황판 계산 신뢰성 확보
4. 출력물 완성도 확보
5. 멀티테넌트 보안 확보

---

## 23. 우선 검토 필요 사항

개발 착수 전 추가 검토 권장 항목:
- Drag & Drop 라이브러리 선택 (예: dnd-kit)
- PDF 생성 방식 결정
- Firestore document 크기 제한 고려한 plans 구조 설계
- 학생 수가 많을 때 계산 성능 최적화 전략
- 작업본 복제 시 참조/복사 기준 정의
- 공용 마스터 데이터 수정 권한 범위 정리

---

## 24. 최종 한줄 정의

**“고등학교 선택과목 시험 운영을 위해, 사용자가 시험 일정을 직접 배치하면 학생 충돌·대기·무시험·고사실 운영 위험을 실시간 분석하고 출력물까지 생성하는 멀티테넌트 웹 기반 시험 계획 자동화 시스템”**
